
const initialState = {
    friends: [
      {id: 0, name: "Olga"},
      {id: 1, name: "Karina"},
      {id: 2, name: "Laura"}
    ]
  }

const reducerSidebar = (state = initialState, action) => {
    
    return state;
}

export default reducerSidebar;